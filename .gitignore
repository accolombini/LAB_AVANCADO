# regras úteis p/ macOS, VSCode, Node, .NET, Python, Arduino
# sobrescrever .gitignore com uma versão consolidada
cat > .gitignore <<'EOF'
# macOS (apenas na raiz)
.DS_Store
Icon?
*.icloud

# IDEs (apenas na raiz)
.vscode/
.idea/
.vs/
EOF

git add .gitignore
git commit -m "chore: slim root .gitignore; project-specific ignores live in each folder"
